@model IEnumerable<JomMalaysia.Presentation.Models.CategoryViewModel>
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutIndex.cshtml";
}

<!--HEADER-->


<div class="col-md-12 col-sm-12 no-padding" style="overflow-x:auto !important" ;">
    <table class="table table-hover">
        <thead class="table-primary">
            <tr>
                <th scope="col"></th>
                <th scope="col">Category</th>
                <th scope="col">Company Name</th>
                <th scope="col">Company Address</th>
                <th scope="col">Category Name</th>
                <th scope="col">Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @*<input type="checkbox" asp-for="@item.selected" />*@
                        @*<div class="custom-control custom-checkbox">
                                <input type="checkbox" class="custom-control-input" id="chkdt1" checked="">
                                <label class="custom-control-label" for="customCheck1"></label>
                            </div>*@
                    </td>
                    <td>@item.CategoryCode</td>
                    <td>@item.CategoryName</td>
                    <td>@item.CategoryNameMs</td>
                    <td>@item.CategoryNameZh</td>
                    
                    <td>
                        <a asp-controller="category" asp-action="delete" asp-route-id="@item.CategoryName">Del</a>
                        <a href="@Url.Action("Delete","Category",new { CategoryName = item.CategoryName })" >Delete</a>
                        @Html.ActionLink("Delete","Delete","Category",new {CategoryName = item.CategoryName})
                       <div class="form-group col-md-12 no-padding d-none d-md-block">
                            <a class="btn btn-danger col-md-2" asp-action="Delete" 
                            asp-controller="Category" asp-route-id = "@item.CategoryName"Delete></a>
                       </div>
        </td>
                </tr>
            }
        </tbody>
    </table>
</div>
